import React, { useState, useEffect } from 'react';
import { Monitor, Cpu, Battery, Zap, ShieldCheck, Headphones, Menu, X, ArrowRight, Instagram, Facebook, Linkedin } from 'lucide-react';

// Componente Principal
export default function App() {
  const [activeSection, setActiveSection] = useState('home');
  const [isMenuOpen, setIsMenuOpen] = useState(false);
  const [scrolled, setScrolled] = useState(false);

  // Efecto para el navbar al hacer scroll
  useEffect(() => {
    const handleScroll = () => {
      setScrolled(window.scrollY > 50);
    };
    window.addEventListener('scroll', handleScroll);
    return () => window.removeEventListener('scroll', handleScroll);
  }, []);

  const navigateTo = (section) => {
    setActiveSection(section);
    setIsMenuOpen(false);
    window.scrollTo({ top: 0, behavior: 'smooth' });
  };

  // Datos extraídos del PDF (Ficticios para la web)
  const products = {
    prime: {
      title: "PrimeLine",
      tagline: "Rendimiento sin límites para los más exigentes.",
      description: "Diseñado para gamers y profesionales creativos. PrimeLine representa la cúspide de Zenith Digital: potencia bruta, componentes premium y una experiencia inmersiva.",
      features: ["Procesadores de última generación", "Gráficos RTX Serie 50", "Refrigeración Líquida ZenithFlow", "Chasis de Aleación Premium"],
      price: "Desde 1.899€",
      theme: "dark"
    },
    eco: {
      title: "EcoLine",
      tagline: "Eficiencia inteligente para tu día a día.",
      description: "Funcionalidad, equilibrio y accesibilidad. EcoLine es la elección perfecta para estudiantes y tareas cotidianas, con la fiabilidad que define a Zenith.",
      features: ["Batería de larga duración (12h+)", "Diseño ultraligero", "Pantallas Eye-Care", "Materiales reciclados"],
      price: "Desde 499€",
      theme: "light"
    }
  };

  return (
    <div className="font-sans antialiased text-slate-800 bg-white min-h-screen flex flex-col">
      
      {/* NAVBAR */}
      <nav className={`fixed w-full z-50 transition-all duration-300 ${scrolled || activeSection === 'prime' ? 'bg-slate-900 text-white shadow-lg' : 'bg-white/90 backdrop-blur-md text-slate-900 shadow-sm'}`}>
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="flex justify-between items-center h-20">
            {/* Logo */}
            <div 
              className="flex-shrink-0 cursor-pointer flex items-center gap-2" 
              onClick={() => navigateTo('home')}
            >
              {/* --- CAMBIO 1: AQUÍ VA TU LOGO DE LA BARRA DE NAVEGACIÓN --- */}
              {/* Si tienes una imagen, borra el código de abajo y usa: <img src="tu-logo.png" alt="Zenith Digital" className="h-10" /> */}
              
              <div className={`w-8 h-8 rounded-sm flex items-center justify-center font-bold text-xl ${activeSection === 'prime' || scrolled ? 'bg-purple-600' : 'bg-blue-600'} text-white`}>
                Z
              </div>
              <span className="font-bold text-2xl tracking-tighter">ZENITH DIGITAL</span>
              
              {/* ----------------------------------------------------------- */}
            </div>

            {/* Desktop Menu */}
            <div className="hidden md:flex space-x-8 items-center">
              <button onClick={() => navigateTo('home')} className="hover:text-blue-500 transition-colors font-medium">Inicio</button>
              <button onClick={() => navigateTo('prime')} className="hover:text-purple-400 transition-colors font-medium">PrimeLine</button>
              <button onClick={() => navigateTo('eco')} className="hover:text-teal-500 transition-colors font-medium">EcoLine</button>
              <button onClick={() => navigateTo('support')} className="hover:text-blue-500 transition-colors font-medium">Soporte</button>
              <button className={`px-5 py-2 rounded-full font-bold transition-all ${activeSection === 'prime' ? 'bg-purple-600 hover:bg-purple-700' : 'bg-blue-600 hover:bg-blue-700 text-white'}`}>
                Tienda Online
              </button>
            </div>

            {/* Mobile Menu Button */}
            <div className="md:hidden flex items-center">
              <button onClick={() => setIsMenuOpen(!isMenuOpen)}>
                {isMenuOpen ? <X size={28} /> : <Menu size={28} />}
              </button>
            </div>
          </div>
        </div>

        {/* Mobile Menu Panel */}
        {isMenuOpen && (
          <div className="md:hidden bg-slate-900 text-white absolute w-full p-4 flex flex-col gap-4 shadow-xl">
            <button onClick={() => navigateTo('home')} className="text-left py-2 border-b border-slate-700">Inicio</button>
            <button onClick={() => navigateTo('prime')} className="text-left py-2 border-b border-slate-700 text-purple-400 font-bold">PrimeLine</button>
            <button onClick={() => navigateTo('eco')} className="text-left py-2 border-b border-slate-700 text-teal-400 font-bold">EcoLine</button>
            <button onClick={() => navigateTo('support')} className="text-left py-2">Soporte</button>
          </div>
        )}
      </nav>

      {/* CONTENT SWITCHER */}
      <main className="flex-grow pt-20">
        
        {/* --- HOME PAGE --- */}
        {activeSection === 'home' && (
          <>
            {/* Hero Section */}
            <section className="relative h-[600px] flex items-center justify-center overflow-hidden bg-slate-900">
              <div className="absolute inset-0 bg-gradient-to-r from-slate-900 via-slate-800 to-blue-900 opacity-90"></div>
              <div className="relative z-10 text-center px-4 max-w-4xl mx-auto">
                <span className="text-blue-400 font-bold tracking-widest uppercase text-sm mb-4 block">Innovación y Rendimiento</span>
                
                {/* --- CAMBIO 2: AQUÍ VA TU ESLOGAN PRINCIPAL --- */}
                <h1 className="text-5xl md:text-7xl font-extrabold text-white mb-6 leading-tight">
                  Technology at <span className="text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-purple-500">Heights</span>
                </h1>
                {/* ---------------------------------------------- */}

                <p className="text-slate-300 text-xl mb-10 max-w-2xl mx-auto">
                  Descubre la diferencia Zenith. Desde la potencia extrema para creadores hasta la eficiencia inteligente para el día a día.
                </p>
                <div className="flex flex-col sm:flex-row gap-4 justify-center">
                  <button onClick={() => navigateTo('prime')} className="px-8 py-4 bg-purple-600 hover:bg-purple-700 text-white rounded-lg font-bold flex items-center justify-center gap-2 transition-all">
                    Descubrir PrimeLine <Zap size={20} />
                  </button>
                  <button onClick={() => navigateTo('eco')} className="px-8 py-4 bg-transparent border-2 border-white text-white hover:bg-white hover:text-slate-900 rounded-lg font-bold flex items-center justify-center gap-2 transition-all">
                    Explorar EcoLine <Battery size={20} />
                  </button>
                </div>
              </div>
            </section>

            {/* Value Propositions (Based on PDF Objectives) */}
            <section className="py-20 bg-white">
              <div className="max-w-7xl mx-auto px-4">
                <div className="text-center mb-16">
                  <h2 className="text-3xl font-bold text-slate-900">Por qué elegir Zenith Digital</h2>
                  <p className="text-slate-600 mt-4">Nuestro compromiso con la calidad y el soporte técnico nos distingue.</p>
                </div>
                <div className="grid md:grid-cols-3 gap-10">
                  <div className="p-8 rounded-2xl bg-slate-50 hover:shadow-xl transition-shadow">
                    <div className="w-14 h-14 bg-blue-100 text-blue-600 rounded-xl flex items-center justify-center mb-6">
                      <ShieldCheck size={32} />
                    </div>
                    <h3 className="text-xl font-bold mb-3">Fiabilidad Garantizada</h3>
                    <p className="text-slate-600">Cada equipo Zenith pasa por rigurosos controles de calidad para asegurar una durabilidad excepcional.</p>
                  </div>
                  <div className="p-8 rounded-2xl bg-slate-50 hover:shadow-xl transition-shadow">
                    <div className="w-14 h-14 bg-purple-100 text-purple-600 rounded-xl flex items-center justify-center mb-6">
                      <Cpu size={32} />
                    </div>
                    <h3 className="text-xl font-bold mb-3">Rendimiento Superior</h3>
                    <p className="text-slate-600">Componentes de vanguardia optimizados para ofrecer la mejor experiencia, ya sea en gaming o estudio.</p>
                  </div>
                  <div className="p-8 rounded-2xl bg-slate-50 hover:shadow-xl transition-shadow">
                    <div className="w-14 h-14 bg-teal-100 text-teal-600 rounded-xl flex items-center justify-center mb-6">
                      <Headphones size={32} />
                    </div>
                    <h3 className="text-xl font-bold mb-3">Soporte Experto</h3>
                    <p className="text-slate-600">Asesoramiento preventa y postventa personalizado. No solo vendemos ordenadores, ofrecemos soluciones.</p>
                  </div>
                </div>
              </div>
            </section>
          </>
        )}

        {/* --- PRIMELINE PAGE --- */}
        {activeSection === 'prime' && (
          <div className="bg-slate-900 text-white min-h-screen">
            <div className="max-w-7xl mx-auto px-4 py-20 grid md:grid-cols-2 gap-12 items-center">
              <div>
                <span className="text-purple-400 font-bold tracking-wider text-sm uppercase mb-2 block">Gama Alta / Gaming</span>
                <h1 className="text-5xl md:text-7xl font-black mb-6 italic">PRIME<span className="text-purple-500">LINE</span></h1>
                <p className="text-xl text-slate-300 mb-8 leading-relaxed">{products.prime.description}</p>
                
                <ul className="space-y-4 mb-10">
                  {products.prime.features.map((feat, idx) => (
                    <li key={idx} className="flex items-center gap-3 text-lg">
                      <div className="bg-purple-600/20 p-2 rounded-lg text-purple-400"><Zap size={20}/></div>
                      {feat}
                    </li>
                  ))}
                </ul>

                <div className="flex items-center gap-6">
                   <button className="bg-purple-600 hover:bg-purple-500 text-white px-8 py-4 rounded-lg font-bold text-lg transition-all shadow-[0_0_20px_rgba(147,51,234,0.5)]">
                    Configurar PrimeLine
                  </button>
                  <p className="text-2xl font-bold text-slate-400">{products.prime.price}</p>
                </div>
              </div>
              <div className="relative h-[500px] bg-slate-800 rounded-3xl overflow-hidden flex items-center justify-center border border-slate-700 shadow-2xl">
                 {/* Placeholder Visual */}
                 <div className="absolute inset-0 bg-gradient-to-tr from-purple-900/40 to-transparent"></div>
                 <div className="text-center z-10">
                    <Monitor size={120} className="text-slate-600 mx-auto mb-4 opacity-50" />
                    <p className="text-slate-500 font-mono">Render del Producto PrimeLine</p>
                    <p className="text-xs text-slate-600 mt-2">Modelo High-End con RGB</p>
                 </div>
              </div>
            </div>

            {/* Especificaciones Técnicas (Mockup) */}
            <div className="bg-slate-950 py-16 border-t border-slate-800">
                <div className="max-w-7xl mx-auto px-4 text-center">
                    <h3 className="text-3xl font-bold mb-12">Especificaciones Máximas</h3>
                    <div className="grid grid-cols-2 md:grid-cols-4 gap-8">
                        <div className="p-6 border border-slate-800 rounded-xl">
                            <p className="text-slate-500 text-sm mb-2">Procesador</p>
                            <p className="text-2xl font-bold text-purple-400">Intel Core Ultra 9</p>
                        </div>
                        <div className="p-6 border border-slate-800 rounded-xl">
                            <p className="text-slate-500 text-sm mb-2">Gráfica</p>
                            <p className="text-2xl font-bold text-purple-400">NVIDIA RTX 4090</p>
                        </div>
                        <div className="p-6 border border-slate-800 rounded-xl">
                            <p className="text-slate-500 text-sm mb-2">RAM</p>
                            <p className="text-2xl font-bold text-purple-400">64GB DDR5</p>
                        </div>
                        <div className="p-6 border border-slate-800 rounded-xl">
                            <p className="text-slate-500 text-sm mb-2">Almacenamiento</p>
                            <p className="text-2xl font-bold text-purple-400">4TB NVMe Gen5</p>
                        </div>
                    </div>
                </div>
            </div>
          </div>
        )}

        {/* --- ECOLINE PAGE --- */}
        {activeSection === 'eco' && (
          <div className="bg-white text-slate-800 min-h-screen">
             <div className="max-w-7xl mx-auto px-4 py-20 grid md:grid-cols-2 gap-12 items-center">
               <div className="order-2 md:order-1 relative h-[500px] bg-teal-50 rounded-3xl overflow-hidden flex items-center justify-center border border-teal-100">
                 <div className="text-center z-10">
                    <Monitor size={120} className="text-teal-200 mx-auto mb-4" />
                    <p className="text-teal-600 font-medium">Diseño EcoLine</p>
                    <p className="text-xs text-teal-400 mt-2">Ligero y Elegante</p>
                 </div>
              </div>

              <div className="order-1 md:order-2">
                <span className="text-teal-600 font-bold tracking-wider text-sm uppercase mb-2 block">Estudiantes / Hogar</span>
                <h1 className="text-5xl md:text-7xl font-bold mb-6 text-slate-900">Eco<span className="text-teal-500">Line</span></h1>
                <p className="text-xl text-slate-600 mb-8 leading-relaxed">{products.eco.description}</p>
                
                <ul className="space-y-4 mb-10">
                  {products.eco.features.map((feat, idx) => (
                    <li key={idx} className="flex items-center gap-3 text-lg text-slate-700">
                      <div className="bg-teal-100 p-2 rounded-full text-teal-600"><ShieldCheck size={20}/></div>
                      {feat}
                    </li>
                  ))}
                </ul>

                <div className="flex items-center gap-6">
                   <button className="bg-teal-600 hover:bg-teal-700 text-white px-8 py-4 rounded-full font-bold text-lg transition-all shadow-lg">
                    Comprar EcoLine
                  </button>
                  <p className="text-2xl font-bold text-teal-700">{products.eco.price}</p>
                </div>
              </div>
            </div>

             <div className="bg-teal-50 py-16">
                <div className="max-w-5xl mx-auto px-4 text-center">
                    <h3 className="text-2xl font-bold text-slate-800 mb-6">Perfecto para la vida en movimiento</h3>
                    <p className="text-slate-600 mb-8">Inspirado en la tecnología de PrimeLine, pero adaptado a lo que realmente necesitas. Sin gastos innecesarios, solo rendimiento puro y eficiencia energética.</p>
                    <div className="flex justify-center gap-4">
                        <span className="px-4 py-2 bg-white rounded-full shadow-sm text-sm font-medium text-slate-600">Windows 11 Home</span>
                        <span className="px-4 py-2 bg-white rounded-full shadow-sm text-sm font-medium text-slate-600">Wi-Fi 6E</span>
                        <span className="px-4 py-2 bg-white rounded-full shadow-sm text-sm font-medium text-slate-600">Carga Rápida USB-C</span>
                    </div>
                </div>
            </div>
          </div>
        )}

        {/* --- SUPPORT / CONTACT PAGE --- */}
        {activeSection === 'support' && (
          <div className="bg-slate-50 min-h-screen py-20">
            <div className="max-w-3xl mx-auto px-4">
              <div className="text-center mb-12">
                <h1 className="text-4xl font-bold text-slate-900 mb-4">¿Necesitas ayuda para elegir?</h1>
                <p className="text-xl text-slate-600">Nuestro enfoque es consultivo. Cuéntanos para qué usarás tu Zenith y te diremos cuál es el modelo perfecto.</p>
              </div>

              <div className="bg-white p-8 rounded-2xl shadow-lg">
                <form className="space-y-6">
                  <div>
                    <label className="block text-sm font-medium text-slate-700 mb-1">Nombre Completo</label>
                    <input type="text" className="w-full p-3 border border-slate-200 rounded-lg focus:ring-2 focus:ring-blue-500 outline-none" placeholder="Tu nombre" />
                  </div>
                  <div>
                    <label className="block text-sm font-medium text-slate-700 mb-1">Correo Electrónico</label>
                    <input type="email" className="w-full p-3 border border-slate-200 rounded-lg focus:ring-2 focus:ring-blue-500 outline-none" placeholder="tucorreo@ejemplo.com" />
                  </div>
                  <div>
                    <label className="block text-sm font-medium text-slate-700 mb-1">¿Cuál es tu uso principal?</label>
                    <select className="w-full p-3 border border-slate-200 rounded-lg focus:ring-2 focus:ring-blue-500 outline-none">
                      <option>Gaming de alto rendimiento</option>
                      <option>Diseño Gráfico / Edición de Video</option>
                      <option>Estudios / Universidad</option>
                      <option>Uso doméstico general</option>
                      <option>Trabajo de oficina</option>
                    </select>
                  </div>
                  <div>
                    <label className="block text-sm font-medium text-slate-700 mb-1">Mensaje (Opcional)</label>
                    <textarea className="w-full p-3 border border-slate-200 rounded-lg focus:ring-2 focus:ring-blue-500 outline-none h-32" placeholder="Cuéntanos más sobre tus necesidades..."></textarea>
                  </div>
                  <button type="button" className="w-full bg-blue-600 text-white font-bold py-4 rounded-lg hover:bg-blue-700 transition-colors">
                    Solicitar Asesoramiento Gratuito
                  </button>
                  <p className="text-xs text-slate-400 text-center mt-4">
                    Al enviar este formulario aceptas recibir comunicaciones de Zenith Digital sobre novedades y ofertas (Email Marketing).
                  </p>
                </form>
              </div>

              <div className="mt-12 grid md:grid-cols-2 gap-6">
                 <div className="bg-white p-6 rounded-xl shadow-sm flex items-center gap-4">
                    <div className="bg-green-100 p-3 rounded-full text-green-600"><Instagram size={24}/></div>
                    <div>
                        <p className="font-bold text-slate-800">Instagram</p>
                        <p className="text-sm text-slate-500">Síguenos para ver setups</p>
                    </div>
                 </div>
                 <div className="bg-white p-6 rounded-xl shadow-sm flex items-center gap-4">
                    <div className="bg-blue-100 p-3 rounded-full text-blue-600"><Linkedin size={24}/></div>
                    <div>
                        <p className="font-bold text-slate-800">LinkedIn</p>
                        <p className="text-sm text-slate-500">Zenith Digital Corporate</p>
                    </div>
                 </div>
              </div>
            </div>
          </div>
        )}

      </main>

      {/* FOOTER */}
      <footer className="bg-slate-900 text-slate-400 py-12 border-t border-slate-800">
        <div className="max-w-7xl mx-auto px-4 grid md:grid-cols-4 gap-8">
          <div>
            <div className="flex items-center gap-2 text-white mb-4">
              
              {/* --- CAMBIO 3: AQUÍ VA TU LOGO DEL FOOTER --- */}
              {/* Igual que arriba, puedes cambiar esto por <img src="tu-logo.png" className="h-8" /> */}
              <div className="w-6 h-6 bg-blue-600 rounded-sm flex items-center justify-center font-bold text-xs">Z</div>
              <span className="font-bold text-lg">ZENITH DIGITAL</span>
              {/* -------------------------------------------- */}

            </div>
            <p className="text-sm mb-4">Technology at heights.</p>
            <div className="flex gap-4">
                <Facebook size={20} className="hover:text-white cursor-pointer"/>
                <Instagram size={20} className="hover:text-white cursor-pointer"/>
                <Linkedin size={20} className="hover:text-white cursor-pointer"/>
            </div>
          </div>
          
          <div>
            <h4 className="text-white font-bold mb-4">Productos</h4>
            <ul className="space-y-2 text-sm">
              <li onClick={() => navigateTo('prime')} className="hover:text-purple-400 cursor-pointer">PrimeLine</li>
              <li onClick={() => navigateTo('eco')} className="hover:text-teal-400 cursor-pointer">EcoLine</li>
              <li className="hover:text-white cursor-pointer">Accesorios</li>
            </ul>
          </div>

          <div>
            <h4 className="text-white font-bold mb-4">Compañía</h4>
            <ul className="space-y-2 text-sm">
              <li className="hover:text-white cursor-pointer">Sobre Nosotros</li>
              <li className="hover:text-white cursor-pointer">Blog de Tecnología</li>
              <li className="hover:text-white cursor-pointer">Carreras</li>
            </ul>
          </div>

          <div>
             <h4 className="text-white font-bold mb-4">Newsletter</h4>
             <p className="text-xs mb-4">Recibe ofertas exclusivas para PrimeLine y EcoLine.</p>
             <div className="flex">
                <input type="email" placeholder="Email" className="bg-slate-800 border-none text-sm px-3 py-2 rounded-l-md w-full focus:ring-1 focus:ring-blue-500 outline-none text-white"/>
                <button className="bg-blue-600 text-white px-3 py-2 rounded-r-md hover:bg-blue-700 font-bold text-sm">OK</button>
             </div>
          </div>
        </div>
        <div className="max-w-7xl mx-auto px-4 mt-12 pt-8 border-t border-slate-800 text-center text-xs">
          <p>© {new Date().getFullYear()} Zenith Digital. Todos los derechos reservados. Proyecto Ficticio.</p>
        </div>
      </footer>
    </div>
  );
}
